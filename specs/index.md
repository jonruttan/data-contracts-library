# Specs Index

Canonical shared surfaces:

- `/specs/05_libraries/**`
- `/specs/06_overlays/**`

Ownership model:

- shared reusable libraries and runner overlays live in this repository
- runner behavior specs remain in `dc-runner-spec`
- contract-spec schema authority remains in `data-contracts`
